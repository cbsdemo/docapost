name: offer_name
type: object
key: OFFER_NAME
data:
  count: 500
  pre_run: |
    globals.doc_count = 0;
properties:
  OFFER_NAME:
    type: string
    data:
      build: "'OFFER_' + ++globals.doc_count"
