name: creator_username
type: object
key: CREATOR_USERNAME
data:
  count: 1030
  pre_run: |
    globals.doc_count = 0;
properties:
  CREATOR_USERNAME:
    type: string
    data:
      build: "'USER_' + ++globals.doc_count"
